{
  "name": "sparta-news-extractor",
  "version": "1.0.0",
  "description": "Automated pipeline for extracting historical data from newspaper scans.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "fs-extra": "^11.1.1"
  }
}
